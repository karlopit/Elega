[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    api: tests that call the FastAPI service
    ui: tests that drive the Next.js application in a browser
    smoke: release-blocking happy-path checks
